// const cron = require("node-cron");

// const checkUserConnection = require("./cronjobs");

// // Schedule a cron job to run every 50 minutes
// cron.schedule("*/50 * * * *", async () => {
//     await checkUserConnection();
// });